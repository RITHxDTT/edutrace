interface ProfileData {
    payload: {
        firstName: string;
        lastName: string;
        email: string;
        role: string;
        username: string;
        fullName: string;
        profileImageUrl: string
    }
}

export type { ProfileData }